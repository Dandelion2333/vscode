操作方法类别：
	1、对行操作
		1、重开一行：当光标不在行位，则使用Ctrl+enter。
			同理Ctrl+shift+enter向上开一行 -+	
			
权限问题：
	1、通过vscode远程连接Ubuntu，想要创建或保存文件时，总是提示没有权限
		因为该文件默认是只有root权限可以写。所以把该文件所在的文件夹的执行权限改为普通用户也有权限
		sudo chown -R $USER 文件夹名